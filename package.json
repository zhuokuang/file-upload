{
  "name": "file-upload",
  "version": "1.0.0",
  "description": "",
  "main": "index.html",
  "scripts": {
    "start": "node server/index.js --port 3000 | npx http-server src/ --port 8080",
    "watch": "node server/index.js --port 3000",
    "server": "npx http-server src/ --port 8080"
  },
  "author": "kz",
  "license": "ISC",
  "dependencies": {
    "fs-extra": "^10.0.0",
    "multiparty": "^4.2.3"
  },
  "devDependencies": {}
}
